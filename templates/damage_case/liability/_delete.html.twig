<form method="post" action="{{ path('damageCase_liability_delete', {'id': entry.id}) }}" onsubmit="return confirm('Soll die Schadenanzeige wirklich gelöscht werden?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entry.id) }}">
    <button class="btn red delete">löschen</button>
</form>